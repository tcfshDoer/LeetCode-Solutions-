En:Implemented a Two-Pointer approach with skipping logic: area is only recalculated when a taller barrier is encountered, eliminating redundant operations and achieving peak \(O(n)\) performance with \(O(1)\) space.

Cn:利用雙指針技術並加入跳躍式優化：僅在遇到比當前邊界更高的木板時才重新計算面積，有效剔除無效運算，達成 \(O(n)\) 時間與 \(O(1)\) 空間的極致效能。
