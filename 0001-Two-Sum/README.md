EN:Leveraged a Hash Map to optimize complexity from \(O(n^{2})\) to \(O(n)\), with the core logic being to let target values 'find us' rather than actively searching for them.

CN:利用哈希表將 \(O(n^{2})\) 降為 \(O(n)\)，核心在於不主動搜尋，讓對象主動找上門。
