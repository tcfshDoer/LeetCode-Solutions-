EN:Implemented an (O(1)) solution by leveraging bitwise XOR to convert alternating bits into a power-of-two sequence, demonstrating a deep understanding of C++ operator overloading and bit manipulation.

CN:利用位元異或將交替位轉化為 2 的冪次方序列，實現了 (O(1)) 解法，展現了對 C++ 運算子多重定義與位元操作的深刻理解。
